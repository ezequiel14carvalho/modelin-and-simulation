# 1. Importando a biblioteca com o apelido padrão 'np'
import numpy as np

# 2. Criando uma lista de números (array) para testar
numeros = np.array([10, 20, 30, 40, 50])

# 3. Usando a função de média que vimos antes
resultado_media = np.mean(numeros)

# 4. Exibindo o resultado na tela
print("O NumPy foi importado com sucesso!")
print("A média dos números é:", resultado_media)